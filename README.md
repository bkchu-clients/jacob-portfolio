#Jacob Kim Product Designer

## Branching Strategy
- `master` is main development branch
- create `feature/...` branch off of `master` to password-protect, then merge into `prod`
- `prod/password-protected` is the main branch that gets deployed